@page "{id:Guid?}"
@using MyApp.WebApp.Pages.Shared.EditorTemplates
@model CloseModel

@{
    ViewData["Title"] = $"Close Entry ID {Model.EntryDto.WorkEntryId}";
    ViewData["NoAnchors"] = "True";
}

<h1>@ViewData["Title"]</h1>

<div class="p-3 border rounded-3">
    <partial name="Shared/_WorkEntrySummaryPartial" model="Model.ItemView" />

    <form method="post">
        <div class="mb-3">
            @Html.EditorFor(model => model.EntryDto.Comments, EditorTemplate.Textarea, new { Rows = 3 })
        </div>
        <div class="mt-3 mb-1">
            <button type="submit" class="btn btn-primary col-5 col-sm-3 col-lg-2 me-2">Close</button>
            <a asp-page="Details" asp-route-id="@Model.EntryDto.WorkEntryId.ToString()" class="btn btn-outline-secondary">Cancel</a>
        </div>
        <input asp-for="EntryDto.WorkEntryId" type="hidden" />
    </form>
</div>

@section Scripts {
    <script src="~/js/EasyMDE.js" defer></script>
    <script src="~/js/textarea-to-MDE.js" defer></script>
}
@section Styles {
    <link rel="stylesheet" href="~/css/EasyMDE.css"/>
}